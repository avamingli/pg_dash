# Direct backend URL for the browser.
# In dev mode Vite proxy handles forwarding — leave blank to use relative paths.
# Only fill these if the browser needs to connect to the backend directly (bypassing proxy).
VITE_API_URL=
VITE_WS_URL=
